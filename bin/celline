#!/bin/bash
EXEC_DIR=$(dirname "$(
    cd "$(dirname "$0")" || exit
    pwd
)")
PROJ_DIR="$(pwd)"

cd "${EXEC_DIR}" || exit
python -m celline "${EXEC_DIR}" "${PROJ_DIR}" "${1}" "${2}" "${3}" "${4}" "${5}"
